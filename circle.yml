machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t mv-free/spike-circle-ci .

test:
  override:
    - docker run -d -p 5000:5000 mv-free/spike-circle-ci && sleep 5
    - curl --retry 10 --retry-delay 5 -v http://localhost:5000

